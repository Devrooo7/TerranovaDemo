<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TerranovaDemo"
             x:Class="TerranovaDemo.SettingsPage"
             BackgroundColor="LightGray"
             Title="Configuración">

    <Grid RowDefinitions="*,Auto">

        <VerticalStackLayout Padding="20" Spacing="20" HorizontalOptions="Center">

            <Label Text="Configuración del sistema"
                   TextColor="Black"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>

            <!-- Estado de conexión ESP32 -->
            <Label x:Name="ConnectionStatus" 
                   Text="Estado: Buscando ESP32..."
                   TextColor="Orange"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   FontSize="18"/>

            <!-- Botón conectar manualmente (opcional) -->
            <Button x:Name="ConnectButton"
                    Text="🔄 Reconectar ESP32"
                    Clicked="ConnectESP32_Clicked"
                    BackgroundColor="#4CAF50"
                    TextColor="White"
                    Padding="20,10"
                    WidthRequest="250"
                    CornerRadius="10"
                    HorizontalOptions="Center"/>

            <!-- Entrada Número de teléfono -->
            <Entry x:Name="PhoneNumberEntry"
                   Placeholder="Número de teléfono"
                   Keyboard="Telephone"
                   WidthRequest="250"
                   HorizontalOptions="Center"/>

            <!-- Botones de acción -->
            <Button Text="💾 Guardar Configuración"
                    Clicked="SavePhoneButton_Clicked"
                    BackgroundColor="Green"
                    TextColor="White"
                    Padding="20,10"
                    WidthRequest="250"
                    HorizontalOptions="Center"/>

            <Button Text="🚪 Cerrar Sesión"
                    Clicked="LogoutButton_Clicked"
                    BackgroundColor="Gray"
                    TextColor="White"
                    Padding="20,10"
                    WidthRequest="250"
                    HorizontalOptions="Center"/>

            <Button Text="🗑️ Eliminar Cuenta"
                    Clicked="DeleteAccountButton_Clicked"
                    BackgroundColor="Red"
                    TextColor="White"
                    Padding="20,10"
                    WidthRequest="250"
                    HorizontalOptions="Center"/>

        </VerticalStackLayout>

        <!-- Barra inferior -->
        <local:BottomNavBar Grid.Row="1"/>
    </Grid>
</ContentPage>
