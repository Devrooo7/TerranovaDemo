<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:TerranovaDemo"
       x:Class="TerranovaDemo.AppShell"
       Title="TerraNova"
       FlyoutBehavior="Flyout"
       BackgroundColor="White">

    <Shell.Resources>
        <ResourceDictionary>
            <Color x:Key="ShellBackgroundColor">#4CAF50</Color>
            <Color x:Key="ShellTitleColor">White</Color>
            <Color x:Key="ShellForegroundColor">White</Color>
        </ResourceDictionary>
    </Shell.Resources>

    <!-- Menú lateral (mantiene los íconos y rutas) -->
    <FlyoutItem Title="Inicio" Icon="home.png">
        <ShellContent Route="mainpage" ContentTemplate="{DataTemplate pages:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Usuario" Icon="user.png">
        <ShellContent Route="user" ContentTemplate="{DataTemplate pages:UserPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Configuración" Icon="settings.png">
        <ShellContent Route="settings" ContentTemplate="{DataTemplate pages:SettingsPage}" />
    </FlyoutItem>

</Shell>
